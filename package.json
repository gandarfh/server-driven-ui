{
  "name": "root",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "pylon"
  ],
  "scripts": {
    "bootstrap": "yarn install && lerna bootstrap",
    "dev": "lerna run dev",
    "build": "lerna run build",
    "start": "lerna run start"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "4.3.5"
  }
}
